You need only two commands, move to the todo_saas directory and run the following commands: 
`pipenv install`
`pip install pipenv`

This will install all the dependecies from the pipfile and pipfile lock! Hopefully :V
